services:
  - type: web
    name: supply-chain-dashboard
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn dashboard:server --workers 1 --threads 8 --timeout 120
    autoDeploy: true
